# egress positive fixture: valid placeholder-only config (render must succeed).
schema: private-proxy/v1
role: egress
listen-address: 0.0.0.0
port: 443
sni: proxy-egress.<domain>
gateway-node-user: gateway-node-01
gateway-node-password: ${GATEWAY_NODE_PASSWORD_1}
max-users: 2

users:
  - username: "gateway-node-01"
    password: "${GATEWAY_EGRESS_PASSWORD_1}"
