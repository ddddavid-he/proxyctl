# Negative fixture: real (non-placeholder) credential in Git config
# (must reject: secrets never enter the repository or render pipeline).
schema: private-proxy/v1
role: gateway
mixed-port: 7890
bind-address: 127.0.0.1
controller-listen: 127.0.0.1:9090
gateway-egress-user: gateway-node-01
gateway-egress-password: hunter2realvalue99
